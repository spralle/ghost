---
"@ghost-shell/arbiter": minor
---

Add beta join (multi-fact pattern matching), temporal planning (time-aware rules), and accumulate patterns (aggregate reasoning) to the arbiter rule engine. All features are backward-compatible with existing sessions.
