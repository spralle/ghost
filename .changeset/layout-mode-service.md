---
"@ghost-shell/contracts": minor
"@ghost-shell/shell": minor
---

Add Layout Mode Service for adaptive layout support. New rule-based mode resolution with signals (viewport, pointer, hover, orientation), configurable rules engine, hysteresis, debounce, and user override. Publishes layout mode as context facts for the predicate system and exposes LayoutApiService on GhostApi.
