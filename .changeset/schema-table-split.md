---
"@ghost-shell/schema-core": minor
"@ghost-shell/table-from-schema": minor
"@ghost-shell/entity-table": minor
"@ghost-shell/data-table": minor
---

Extract `@ghost-shell/schema-core` with a generic schema ingestion pipeline. Create `@ghost-shell/table-from-schema` for framework-agnostic schema-to-table config. Refactor `entity-table` to use the new table-from-schema pipeline. Extract card view into standalone `EntityCardList` component. Add `ResponsiveEntity` container-width-aware switcher.
