---
"@ghost-shell/table-from-schema": minor
"@ghost-shell/entity-table": minor
"@ghost-shell/data-table": minor
---

Create `@ghost-shell/table-from-schema` for framework-agnostic schema-to-table config. Refactor `entity-table` to use the schema-to-table pipeline. Extract card view into standalone `EntityCardList` component. Add `ResponsiveEntity` container-width-aware switcher.
